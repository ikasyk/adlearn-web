
const Properties = {
/*
    reader: null,

    get: (name) => {
        if (this.reader === null) {
            this.reader = PropertiesReader('././application.properties');
        }

        return this.reader.get(name);
    },*/
    "Auth.ClientId": "adlearnclient",
    "Auth.ClientPassword": "1q2w3e4r5rrg44tt"
};

export default Properties;